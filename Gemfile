# frozen_string_literal: true
# Set string literals to be frozen by default. Magic ruby line.

source 'https://rubygems.org'
#gem 'discordrb', git: 'https://github.com/meew0/discordrb.git'

#gem 'imgkit'
#gem 'nokogiri'
#gem 'sqlite3'

# These are built in core modules
#gem 'open-uri'
#gem 'yaml'

group :development do
  gem 'rubocop'
end
